# Руководство пользователя Fast Ray Gram

> 📖 **Техническая документация:** Для более подробного изучения работы проекта можете обратиться к [технической документации](./technical/README.md).

---

Данное руководство описывает процесс развертывания Fast Ray Gram на одном сервере с использованием общего `docker-compose.yml` файла. Система состоит из следующих компонентов:

- **Backend API** (FastAPI) - основной API сервер
- **Frontend** (React) - веб-интерфейс
- **Notificator** - сервис отправки уведомлений
- **Telegram Bot** - Telegram бот для интеграции
- **PostgreSQL** - база данных
- **Redis** - кэш и хранение токенов (2 инстанса)

**Важно:** 3X-UI v2.8.5 должен быть установлен и настроен на отдельном сервере. Интеграция с 3X-UI происходит через API.

> **💡 Примечание:** Если вам необходимо разнести сервисы по нескольким серверам, используйте отдельные `docker-compose.yml` файлы, расположенные в папках каждого сервиса (`backend/`, `frontend/`, `notificator/`, `tg_bot/`). В этом случае потребуется настроить сетевые подключения между серверами и обновить переменные окружения для указания адресов удаленных сервисов.

### Требования

#### Системные требования

- **ОС:** Ubuntu 22.04 LTS
- **RAM:** минимум 2GB (рекомендуется 4GB+)
- **Диск:** минимум 20GB свободного места
- **Процессор:** 2+ ядра

#### Программное обеспечение

- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше
- **Make** для управления проектом через Makefile
- **Nginx** для проксирования запросов
- **Certbot** для получения SSL сертификатов
- **UFW** (Uncomplicated Firewall) для настройки файрвола

#### Внешние сервисы

- **3X-UI v2.8.5** - должен быть установлен и доступен на отдельном сервере
  - **Fail2Ban должен быть включен** - требуется для безопасности и блокировки IP
  - **Логи для IP лимитинга должны быть включены** - требуется для мониторинга трафика и управления пользователями
  - **Должен быть настроен VLESS инбаунд с remark='vless'** - требуется для автоматического добавления пользователей. Система будет добавлять новых пользователей в этот инбаунд, используя поле remark
- **Доменное имя** - для доступа к приложению через HTTPS

#### Дополнительные сервисы (опционально, только для полного режима)

Для работы дополнительных сервисов (Telegram Bot и Notificator) в **полном режиме** требуется:

- **Telegram Bot Token** - получить у [@BotFather](https://t.me/BotFather) в Telegram
  - Создайте бота через [@BotFather](https://t.me/BotFather)
  - Получите токен бота
  - Укажите токен в переменных окружения (см. раздел "Настройка переменных окружения")
- **URL веб-приложения** - для работы Telegram бота и отправки уведомлений
- **Доступ к базе данных** - Notificator использует ту же базу данных, что и Backend API (настраивается автоматически через общий `.env` файл)

> **💡 Примечание:** В lite режиме сервисы Notificator и Telegram Bot не запускаются, поэтому эти требования не обязательны.

### Подготовка сервера

> **💡 Примечание:** Если у вас уже настроен сервер с установленными Docker, Docker Compose, Make, Nginx и SSL-сертификатом, этот раздел можно пропустить и перейти к [Настройка переменных окружения](#настройка-переменных-окружения).

#### 1. Получение домена

Убедитесь, что у вас есть доменное имя и оно указывает на IP-адрес вашего сервера:

```bash
# Проверьте, что домен указывает на ваш сервер
nslookup your-domain.com
# или
dig your-domain.com
```

#### 2. Установка Make

```bash
# Установка Make
sudo apt update
sudo apt install -y make

# Проверка установки
make --version
```

#### 3. Установка Docker и Docker Compose

Следуйте официальной документации Docker для установки на Ubuntu:

- [Установка Docker Engine на Ubuntu](https://docs.docker.com/engine/install/ubuntu/)
- [Установка Docker Compose](https://docs.docker.com/compose/install/)

После установки убедитесь, что Docker и Docker Compose работают:

```bash
docker --version
docker compose version
```

#### 4. Установка Nginx

```bash
sudo apt update
sudo apt install -y nginx

# Проверка статуса
sudo systemctl status nginx
```

#### 5. Установка Certbot

```bash
sudo apt update
sudo apt install -y certbot python3-certbot-nginx
```

### Настройка переменных окружения

Все сервисы используют единый файл `.env` в корне проекта. Это упрощает управление конфигурацией и исключает конфликты имен переменных.

1. Скопируйте пример файла окружения:

```bash
cd /path/to/fast_ray_gram
cp example_env .env
```

2. Отредактируйте файл `.env` и заполните **обязательные** переменные:

```bash
nano .env
```

#### Обязательные переменные для заполнения

**⚠️ ВАЖНО:** Следующие переменные **обязательно** нужно заменить на реальные значения:

##### Database (обязательно)
- `DB_PASSWORD` - пароль для базы данных PostgreSQL (используйте сильный пароль)

##### Redis (обязательно)
- `REDIS_PASSWORD` - пароль для основного Redis (используется для инициализации контейнера)
- `REDIS_USER_PASSWORD` - пароль для пользователя Redis (используется приложением для подключения)
- `RATE_LIMIT_PASSWORD` - пароль для Redis rate limiter (используется для инициализации контейнера)
- `RATE_LIMIT_USER_PASSWORD` - пароль для пользователя Redis rate limiter (используется приложением)

##### Frontend (обязательно)
- `VITE_API_URL` - URL вашего API (например: `https://your-domain.com/api`)

##### Notificator (обязательно. Опционально для лайт мода)
- `TELEGRAM_WEB_APP_URL` - URL вашего веб-приложения (например: `https://your-domain.com`)

##### Telegram Bot (обязательно. Опционально для лайт мода)
- `BOT_TOKEN` - токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))
- `WEB_APP_URL` - URL вашего веб-приложения (например: `https://your-domain.com`)

##### Backend API (обязательно)
- `AUTH_JWT_MASTER_KEY` - секретный ключ для JWT токенов (**минимум 32 символа**, используйте очень длинный случайный ключ)
- `APP_ALLOWED_DOMAINS` - список разрешенных доменов через запятую (например: `your-domain.com,www.your-domain.com`)
- `APP_SALT` - соль для хеширования (используйте случайную строку длиной 32+ символов)
- `APP_SUPERUSER_PASSWORD` - пароль для суперпользователя (используется для первого входа в систему)
- `APP_TELEGRAM_BOT_TOKEN` - токен Telegram бота (может быть таким же, как `BOT_TOKEN`)

##### 3X-UI Integration (обязательно)
- `XUI_HOST` - IP-адрес или домен сервера с 3X-UI (например: `192.168.1.100` или `xui.your-domain.com`)
- `XUI_SECRET_PATH` - секретный путь 3X-UI (например: `/your-secret-path-here`)
- `XUI_USERNAME` - имя пользователя для доступа к 3X-UI
- `XUI_PASSWORD` - пароль для доступа к 3X-UI

#### Переменные с дефолтными значениями (можно оставить как есть)

Следующие переменные имеют дефолтные значения и **не требуют** обязательного изменения, если они вас устраивают:

##### Database (опционально)
- `DB_HOST=frg-db` - имя контейнера (не менять для Docker)
- `DB_PORT=5432` - порт PostgreSQL внутри контейнера
- `DB_NAME=frg` - имя базы данных
- `DB_USER=frg_db_admin` - пользователь базы данных

##### Redis (опционально)
- `REDIS_HOST=frg-redis` - имя контейнера (не менять для Docker)
- `REDIS_PORT=6379` - стандартный порт Redis
- `REDIS_DB_NUMBER=0` - номер базы данных Redis
- `REDIS_MAX_CONNECTIONS=100` - максимальное количество соединений
- `REDIS_USER=frg_redis_admin` - имя пользователя Redis

##### Rate Limiter Redis (опционально)
- `RATE_LIMIT_HOST=frg-rate-limiter-redis` - имя контейнера (не менять для Docker)
- `RATE_LIMIT_PORT=6379` - стандартный порт Redis
- `RATE_LIMIT_DB_NUMBER=0` - номер базы данных Redis
- `RATE_LIMIT_MAX_CONNECTIONS=100` - максимальное количество соединений
- `RATE_LIMIT_USER=frg_redis_rate_limiter_admin` - имя пользователя Redis
- `RATE_LIMIT_ENABLED=true` - включить rate limiting
- `RATE_LIMIT_EXCLUDE_IP_ADDRESSES=192.168.65.2,192.168.1.1` - IP адреса, исключенные из rate limiting
- `RATE_LIMIT_DEFAULT_REQUESTS_PER_MINUTE=60` - лимит запросов в минуту по умолчанию

##### Frontend (опционально)
- `VITE_TELEGRAM_GROUP_URL` - ссылка на Telegram группу (можно оставить пустым)
- `VITE_TELEGRAM_BOT_URL` - ссылка на Telegram бота (если пустая, авторизация через Telegram будет недоступна)

##### Notificator (опционально)
- `TELEGRAM_SUPERUSER_ID=0` - ID суперпользователя в Telegram (0 = отключено)
- `APP_CLEANUP_PERIOD_DAYS=2` - период очистки старых уведомлений
- `APP_PROCESS_PERIOD_SEC=10` - период проверки новых уведомлений
- `APP_CONFIG_EXPIRY_NOTIF_HOURS=2` - за сколько часов до истечения отправлять уведомление

##### Authentication (опционально)
- `AUTH_ENABLED=true` - включить аутентификацию
- `AUTH_JWT_ALGORITHM=HS256` - алгоритм подписи JWT
- `AUTH_ACCESS_TOKEN_EXPIRE_SEC=1200` - время жизни access token (20 минут)
- `AUTH_REFRESH_TOKEN_EXPIRE_SEC=3000` - время жизни refresh token (50 минут)
- `AUTH_MAX_SESSIONS=3` - максимальное количество одновременных сессий

##### Application (опционально)
- `APP_SECRET_PATH=/secret_url` - секретный путь для документации API
- `APP_SUPERUSER_LOGIN=admin` - логин суперпользователя
- `APP_DISABLE_REGISTRATION_STARTUP_PARAM=false` - отключить регистрацию новых пользователей
- `APP_MAX_LIMIT_IP_STARTUP_PARAM=10` - максимальное количество IP адресов на пользователя
- `APP_MAX_TOTAL_GB_STARTUP_PARAM=1000` - максимальный трафик в GB
- `APP_BASE_LIMIT_IP=1` - базовое количество IP адресов для нового пользователя
- `APP_BASE_TOTAL_GB=100` - базовый трафик в GB для нового пользователя
- `APP_PROMO_DAYS=7` - количество дней промо-периода
- `APP_EXPIRY_DAYS=30` - срок действия конфигурации в днях

##### 3X-UI Integration (опционально)
- `XUI_SCHEME=http` - протокол (http или https)
- `XUI_PORT=8080` - порт 3X-UI API
- `XUI_SUBSCRIPTION_PORT=2096` - порт для subscription ссылок

#### Пример минимальной конфигурации

Минимальный набор обязательных переменных для запуска:

```bash
# Database - ОБЯЗАТЕЛЬНО
DB_PASSWORD=your_secure_database_password_here

# Redis - ОБЯЗАТЕЛЬНО
REDIS_PASSWORD=your_redis_password_here
REDIS_USER_PASSWORD=your_redis_user_password_here
RATE_LIMIT_PASSWORD=your_rate_limit_redis_password_here
RATE_LIMIT_USER_PASSWORD=your_rate_limit_redis_user_password_here

# Frontend - ОБЯЗАТЕЛЬНО
VITE_API_URL=https://your-domain.com/api

# Notificator - ОБЯЗАТЕЛЬНО
TELEGRAM_WEB_APP_URL=https://your-domain.com

# Telegram Bot - ОБЯЗАТЕЛЬНО
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
WEB_APP_URL=https://your-domain.com

# Backend API - ОБЯЗАТЕЛЬНО
AUTH_JWT_MASTER_KEY=your_very_long_and_secure_jwt_key_min_32_chars_long_at_least
APP_ALLOWED_DOMAINS=your-domain.com,www.your-domain.com
APP_SALT=your_secret_salt_here_random_string_32_chars_minimum
APP_SUPERUSER_PASSWORD=your_superuser_password_here
APP_TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# 3X-UI Integration - ОБЯЗАТЕЛЬНО
XUI_HOST=192.168.1.100
XUI_SECRET_PATH=/your-secret-path-here
XUI_USERNAME=admin
XUI_PASSWORD=your_xui_password_here
```

⚠️ **Рекомендации по безопасности:**
- Используйте **уникальные сильные пароли** для каждой переменной
- `AUTH_JWT_MASTER_KEY` должен быть **минимум 32 символа**, лучше 64+ символов
- `APP_SALT` должен быть случайной строкой длиной 32+ символов
- Не используйте одинаковые пароли для разных сервисов
- Храните файл `.env` в безопасности и не коммитьте его в git (он уже в `.gitignore`)

## Развертывание на сервере

## Настройка Nginx

### Установка Nginx

См. раздел "Быстрый старт" → "Подготовка сервера" → "Установка Nginx".

### Конфигурация Nginx для Fast Ray Gram

1. Скопируйте конфигурационный файл из проекта:

```bash
sudo cp /path/to/project/nginx/fast-ray-gram-with-blocks /etc/nginx/sites-available/fast-ray-gram
```

2. Отредактируйте файл, заменив `your-domain.com` на ваш домен:

```bash
sudo nano /etc/nginx/sites-available/fast-ray-gram
```

Замените все вхождения `your-domain.com` на ваш реальный домен.

3. Добавьте rate limiting зону в основной конфигурационный файл Nginx:

```bash
sudo nano /etc/nginx/nginx.conf
```

В блоке `http` добавьте (если еще не добавлено):

```nginx
limit_req_zone $binary_remote_addr zone=ddos_protection:10m rate=100r/s;
```

4. Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/fast-ray-gram /etc/nginx/sites-enabled/
```

5. Проверьте конфигурацию:

```bash
sudo nginx -t
```

Если проверка прошла успешно, перезагрузите Nginx:

```bash
sudo systemctl reload nginx
```

### Настройка SSL сертификатов (Let's Encrypt)

**Важно:** Перед получением SSL сертификата убедитесь, что:
- Домен указывает на IP-адрес вашего сервера
- Порты 80 и 443 открыты в файрволе
- Nginx запущен и доступен на порту 80

1. Получите SSL сертификат:

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

Certbot автоматически:
- Получит сертификат от Let's Encrypt
- Обновит конфигурацию Nginx для использования HTTPS
- Настроит автоматическое обновление сертификата

2. Проверьте автоматическое обновление:

```bash
sudo certbot renew --dry-run
```

### Настройка файрвола (UFW)

1. Запустите скрипт настройки файрвола:

```bash
cd /path/to/project/ufw
chmod +x ports-bloks.sh
sudo ./ports-bloks.sh
```

Скрипт выполнит следующие действия:
- Разрешит SSH (порт 22)
- Разрешит HTTP (порт 80)
- Разрешит HTTPS (порт 443)
- Разрешит PostgreSQL (порт 5555) - только для внешнего доступа, если необходимо
- Разрешит доступ к портам 3000 и 8080 только с localhost
- Заблокирует внешний доступ к портам 3000 и 8080
- Включит UFW

2. Проверьте статус файрвола:

```bash
sudo ufw status verbose
```

Ожидаемый результат:
- Порты 22, 80, 443, 5555 открыты для всех
- Порты 3000 и 8080 доступны только с localhost

### Запуск проекта

Все сервисы запускаются через Makefile. Убедитесь, что файл `.env` создан и заполнен (см. раздел "Настройка переменных окружения").

Проект поддерживает два режима работы:
- **Полный режим (по умолчанию)** - все сервисы включая Notificator и Telegram Bot
- **Lite режим** - только основные сервисы (Backend API, Frontend, Database, Redis) без Notificator и Telegram Bot

Команды ниже предназначены для **полного режима** (по умолчанию). Если вам нужен lite режим, см. раздел [Light режим](#light-режим) ниже.

#### Основные команды

**Запуск:**
```bash
make up
```

Эта команда:
- Соберет образы для всех сервисов (если необходимо)
- Запустит все контейнеры в фоновом режиме
- Создаст общую сеть для взаимодействия сервисов
- Настроит зависимости между сервисами

**Проверка статуса:**
```bash
make ps
```

Все сервисы должны быть в статусе `Up`:
- `database` - PostgreSQL
- `redis` - основной Redis
- `redis-rate-limiter` - Redis для rate limiting
- `api` - Backend API
- `website` - Frontend
- `notificator` - сервис уведомлений
- `tg-bot` - Telegram бот

**Остановка:**
```bash
make down
```

**Перезапуск:**
```bash
make restart
```

**Полное удаление (включая volumes):**
```bash
make destroy
```

**Обновление проекта:**
```bash
make update        # Обновление с пересборкой (сохраняет данные)
make update-hard   # Обновление с полной пересборкой (удаляет все данные)
```

#### Просмотр логов

```bash
make logs              # Логи всех сервисов
make logs-api          # Логи Backend API
make logs-website      # Логи Frontend
make logs-notificator  # Логи Notificator
make logs-tg-bot       # Логи Telegram Bot
```

#### Дополнительные команды

```bash
make build    # Сборка образов без запуска
make rebuild  # Пересборка и перезапуск всех сервисов
make help     # Показать справку по всем командам
```

#### Light режим

Light режим запускает только основные сервисы (Backend API, Frontend, Database, Redis) без Notificator и Telegram Bot.

Для запуска в light режиме добавьте параметр `mode=lite` к любой команде:

```bash
# Запуск в lite режиме
make up mode=lite

# Остановка в lite режиме
make down mode=lite

# Просмотр логов в lite режиме
make logs mode=lite

# Обновление в lite режиме
make update mode=lite
```

**Примечание:** В lite режиме команды `make logs-notificator` и `make logs-tg-bot` недоступны, так как эти сервисы не запускаются.

**Примеры использования:**

```bash
# Запуск проекта
make up

# Просмотр логов
make logs

# Остановка
make down

# Полное удаление (включая данные)
make destroy

# Обновление проекта из git (сохраняет данные)
make update

# Обновление проекта из git с полной пересборкой (удаляет все данные)
make update-hard

# Запуск в lite режиме (без Notificator и Telegram Bot)
make up mode=lite

# Обновление в lite режиме
make update mode=lite
```

**⚠️ Внимание:** Команда `make destroy` удаляет все данные (база данных, Redis), включая папку `volumes/`. Используйте её только если вы уверены, что хотите удалить все данные.

**⚠️ Внимание:** Команда `make update-hard` выполняет `destroy` перед обновлением, что приведет к потере всех данных. Используйте её для полного обновления проекта с чистой установкой.

### Итого: запуск проекта

Для запуска проекта выполните одну из команд:

```bash
# Полный режим (все сервисы)
make up

# Lite режим (без Notificator и Telegram Bot)
make up mode=lite
```

### Проверка конфигурации и первоначальная настройка

1. Проверьте, что все сервисы запущены:

```bash
# Проверка Docker контейнеров
make ps

# Проверка Nginx
sudo systemctl status nginx

# Проверка портов
sudo netstat -tlnp | grep -E ':(80|443|3000|8080|5555)'
```

2. Дождитесь полной инициализации Backend API (обычно 10-30 секунд). Проверьте логи:

```bash
docker compose logs api | grep -i "started\|ready\|error"
```

Система автоматически создаст:
- Роли (SUPERUSER, ADMIN, USER)
- Суперпользователя с логином и паролем из `APP_SUPERUSER_LOGIN` и `APP_SUPERUSER_PASSWORD`
- Базовые настройки приложения

3. Проверьте доступность через браузер:

- Откройте `https://your-domain.com` - должен открыться фронтенд
- Откройте `https://your-domain.com/api/app/health` - должен вернуться статус API

4. Войдите в систему используя учетные данные суперпользователя:
   - Логин: значение из `APP_SUPERUSER_LOGIN` (по умолчанию `admin`)
   - Пароль: значение из `APP_SUPERUSER_PASSWORD` (тот, который вы указали в `.env`)

5. Проверьте, что порты 3000 и 8080 недоступны извне:

```bash
# С другого сервера попробуйте подключиться (должно не получиться)
curl http://your-server-ip:3000
curl http://your-server-ip:8080
```

## Обслуживание

### Обновление проекта

Для обновления проекта используйте команду `make update`:

```bash
make update
```

Эта команда:
1. Выполняет `git pull` для получения последних изменений
2. Пересобирает и перезапускает все сервисы
3. **Сохраняет все данные** (база данных, Redis)

**Для полного обновления с удалением всех данных:**

```bash
make update-hard
```

Эта команда:
1. Выполняет `git pull` для получения последних изменений
2. Выполняет `make destroy` для полного удаления контейнеров и volumes
3. Пересобирает и запускает все сервисы заново

**⚠️ Внимание:** Команда `make update-hard` удаляет все данные (база данных, Redis). Используйте её только если вы готовы потерять все данные или у вас есть резервная копия.

**Рекомендации:**
- Используйте `make update` для обычных обновлений (сохраняет данные)
- Используйте `make update-hard` только если:
  - Есть критические изменения в схеме базы данных
  - Нужна чистая установка
  - У вас есть резервная копия данных

### Мониторинг логов

#### Логи Docker контейнеров

Для просмотра логов используйте команды Makefile:

```bash
# Логи всех контейнеров в реальном времени
make logs

# Логи конкретного сервиса в реальном времени
make logs-api          # Backend API
make logs-website      # Frontend
make logs-notificator  # Notificator
make logs-tg-bot       # Telegram Bot
```

Все команды показывают логи в реальном времени (следуют за новыми записями). Для выхода нажмите `Ctrl+C`.

#### Логи Nginx

```bash
# Логи доступа
sudo tail -f /var/log/nginx/fast_ray_gram_access.log

# Логи ошибок
sudo tail -f /var/log/nginx/fast_ray_gram_error.log
```
